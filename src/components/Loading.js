import React from "react";
import styled from "styled-components";
export default () => <LoaderWrapper>. . .</LoaderWrapper>;

const LoaderWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  max-height: 500px;
  max-width: 1080px;
  width: 100%;
`;
